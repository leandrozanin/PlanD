import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    container:{
        marginBottom:'100px',
        minHeight:'100%'
    },
    list: {
      width: 250,
    },
    fullList: {
      width: 'auto',
    },
    menuIcon:{
        color: 'white'
    }
});
export default useStyles